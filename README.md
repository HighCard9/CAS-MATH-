# CAS-MATH-

- **One file for handheld / .tns:** paste `nspire/calc2_all_in_one.lua` into a Lua page on CX CAS (see header). Save the document as `.tns`.
- **Modular source:** `src/calc2/`. Desktop tests: `lua5.4 tests/smoke.lua`. Catalog: `docs/CALC2_LUA_FUNCTION_CATALOG.md`.
